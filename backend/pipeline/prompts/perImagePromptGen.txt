Analyze the attached image as the single visual reference.

Detect and lock the subject, environment, composition, camera angle, framing, perspective, mood, textures, materials, colors, and spatial relationships exactly as seen. Do not invent elements that are not visually present.

Generate a **high-fidelity cinematic video prompt** that can be used directly in **Veo or Kling**.

The output must be a **ready-to-use cinematic shot prompt**, written in clear professional English, covering:
- **Aspect ratio and orientation:** mandatory **vertical 9:16**
- **Shot type and framing** (close-up / medium / wide, subject placement within 9:16 frame)
- **Camera movement** (static, slow push-in, dolly, orbit, tracking, tilt, pan — realistic only)
- **Lens and cinematic look** (full-frame feel, focal length style, depth of field)
- **Environment description** strictly derived from the image
- **Subject description** without altering identity, shape, or design
- **Motion behavior** (natural physics, realistic movement)
- **Overall cinematic tone** (grounded, realistic, premium, filmic)

## OUTPUT FORMAT (STRICTLY FOLLOW - JSON)

You MUST output your response as valid JSON with the following structure. Do not include any additional text, explanations, or commentary outside the JSON structure.

```json
{
  "aspect_ratio": "string - mandatory vertical 9:16",
  "shot_type_and_framing": "string - close-up/medium/wide with subject placement within 9:16 frame",
  "camera_movement": "string - choose varied realistic movement (push-in, push-out, pan-left, pan-right, tilt, track, static); do not always use slow push-in",
  "lens_and_cinematic_look": "string - full-frame feel, focal length style, depth of field",
  "environment_description": "string - strictly derived from the image",
  "subject_description": "string - without altering identity, shape, or design",
  "motion_behavior": "string - natural physics, realistic movement",
  "overall_cinematic_tone": "string - grounded, realistic, premium, filmic"
}
```

**Hard constraints (non-negotiable):**

- Preserve the image as the single source of truth.
- Output must be optimized for **vertical 9:16 composition**.
- Camera movement must be varied across different images; avoid repetitive default motion.
- Output MUST be valid JSON following the structure above.
- No sparkles, no glitter, no artificial shine.
- No fantasy, no exaggeration, no dramatic effects.
- No lighting instructions unless they are already implied by the image.
- No text overlays, no logos, no voiceover, no music.
- No emojis.
- Output only the cinematic video prompt in JSON format.
- Do not explain, justify, or add commentary.

The final output must be a **single clean JSON response** containing the cinematic video prompt that can be used directly with a video generation model without modification.
